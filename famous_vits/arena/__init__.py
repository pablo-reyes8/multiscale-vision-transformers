"""Reproducible cross-architecture comparison arena.

Import :func:`famous_vits.arena.engine.compare_models` for the experiment
runner. The package stays lazy so the model factory can import presets without
creating an import cycle.
"""
